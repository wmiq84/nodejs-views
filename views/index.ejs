<!DOCTYPE html>
<html lang="en">
<head>
    <title>Homepage</title>
    <link rel="stylesheet" href="./styles.css">
</head>
<body>
    <ul>
        <% messages.forEach(message => { %>
            <%- include('message', {text: message.text, user: message.user, added: message.added}); %>
        <% }); %>
    </ul>   
</body>
</html>